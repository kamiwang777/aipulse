# Screenshots

Place a PNG named `menubar.png` here showing the menubar line + expanded dropdown.

Recommended: use macOS `Cmd+Shift+4` then `Space` to capture the SwiftBar window, or `Cmd+Shift+5` for region capture.
